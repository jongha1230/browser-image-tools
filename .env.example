# Demo-first mode: leave every value empty.
# Temporary public indexing on the Production vercel.app host:
# set SITE_URL and NEXT_PUBLIC_SITE_URL to the exact Production https://<project>.vercel.app origin
# and set ALLOW_VERCEL_APP_INDEXING=true in the Production environment only.
SITE_URL=
NEXT_PUBLIC_SITE_URL=
ALLOW_VERCEL_APP_INDEXING=
